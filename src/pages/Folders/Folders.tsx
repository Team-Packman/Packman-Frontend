import { AppScreen } from '@/shared/ui/app-screen/AppScreen';
import { DnDTree } from '@/shared/ui/dnd-tree/DnDTree';

const Folders = () => (
  <AppScreen>
    <DnDTree />
  </AppScreen>
);

export { Folders };
