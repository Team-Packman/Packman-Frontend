import styled from '@emotion/styled';

const Divider = styled.div`
  width: var(--mw);
  width: 100%;
  height: 1px;
  margin: 0 -2rem;

  background: var(--gray-gray1, #f2f2f2);
`;

export { Divider };
