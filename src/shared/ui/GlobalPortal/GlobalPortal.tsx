import {
  GlobalPortalConsumer as PortalConsumer,
  GlobalPortalProvider as PortalProvider,
} from './context/GlobalPortalContext';

const GlobalPortal = { PortalConsumer, PortalProvider };

export default GlobalPortal;
